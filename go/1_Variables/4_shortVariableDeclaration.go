package main

import "fmt"


func main() {
	// := it is called the walrus operator and use to shorten the declarations.

	messageStart := "Happy birthday! You are are now "
	age := 21
	messageEnd := " years old!"

	fmt.Println(messageStart, age, messageEnd)
}
